apiVersion: v1
kind: ConfigMap
metadata:
  name: ui-config
  namespace: ai-platform
data:
  # Internal Cluster DNS for your backend API service (replace 'api-service' if different).
  API_URL_CHAT: "http://api-service.ai-platform.svc.cluster.local:8000/chat"
